# Network Configuration for Docker NAT
EXTERNAL_IP=auto
# Set to 'auto' for automatic detection, or specify your public IP manually
# Examples: EXTERNAL_IP=203.0.113.10 or EXTERNAL_IP=192.168.1.100 (for local testing)

# SIP Trunk Configuration
TRUNK_USERNAME=your_sip_trunk_username
TRUNK_PASSWORD=your_sip_trunk_password
TRUNK_HOST=sip.provider.example.com

